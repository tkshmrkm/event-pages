# Claude 引き継ぎ — TechEx Europe・EuroBLECH 2026 field guide v3

更新日: 2026-08-14（ラウンジ集約・会場地図リンク・表の横切れ修正まで完了）

## 目的

`index.html`（フォルダの公開入口）を、HRS公開版と承認済み4列サンプルに沿った現地用ページとして完成させる。

当初の未完了6項目は2026-08-14にすべて反映した。

## 進め方

**2026-08-15以降**: EUROBLECHを参考にしてHRSを作り込み、必要なものをEUROBLECHへ戻す。
参考にする対象がこのフォルダ、作り込む場所がHRS、戻す先がこのフォルダである。

**2026-08-14まで**: このフォルダで仕様を確定し、固まったものをHRSへ戻していた。
この段階で戻したのは、乗り継ぎ・待ち時間の書き方、家族タブの構成、文章の調子、
地図リンクの張り方、交通手段の全アイコン化、日付カードの一括開閉。
CRLF正規化はHRS側にも適用済み。

作り込む場所が入れ替わっただけで、どちらの段階も同じ目的に向かっている。

**このフォルダが受け取る予定の項目**は `../WORK_STATUS.md` の
「HRSからEUROBLECHへ戻す」節にある。HRSが固まるまで着手しない。

**編集の注意**: `build_v3.mjs` は上部の `SOURCE_TEXT_REPLACEMENTS`（置換定義の配列）と
下部の `transformScript`（ブラウザで走るテンプレートリテラル）に同じ文言のコメントが
入ることがある。アンカーを決めたら**出現回数が1件であることを確認してから**当てる。
テンプレートリテラル内では `\n` や `\*` は二重にエスケープする（`\\n` / `\\*`）。
壊れると `Static HRS transformation did not complete` としか出ないので、
原因はブラウザのコンソールを見ないと分からない。

**このファイルが現在の引き継ぎ本体。** `NEXT_SESSION_HANDOFF.md` は初期設計の補足資料であり、内容が異なる場合は本書とユーザーの最新指示を優先する。

## ファイル構成（2026-08-14にHRS方式へそろえた）

v3をフォルダの公開入口にした。HRS（`202609_HumanoidSummitEurope`）と同じ並びである。

| ファイル | 役割 |
| --- | --- |
| `index.html` | **v3の生成物。公開入口。** 直接編集しない |
| `index_v1.html` | 旧版。`build_v3.mjs` と `build_v2.mjs` の入力元 |
| `index_v2.html` | 家族タブの内容・情報構造の基準。`build_v2.mjs` の生成物 |
| `index_v3.html` | 以前の共有URLを `./` へ転送する互換ページ |
| `build_v3.mjs` | `index_v1.html` ＋ `index_v2.html` から `index.html` を生成 |
| `validate_v3.mjs` | `index.html` を検査 |

以前は `index.html` が旧版、`index_v3.html` が生成物だった。**入れ替わっているので、
古い手順書やメモの `index_v3.html` を生成物と読まないこと。**

## 最初に全文確認するファイル

1. `NEXT_SESSION_HANDOFF.md` — Git管理済みの初期設計補足。本書と異なる場合は本書を優先
2. `transport_actual_1017_1018_sample.html` — 4列交通表示の承認サンプル
3. `../202609_HumanoidSummitEurope/index.html` — HRS公開版の構造
4. `../202609_HumanoidSummitEurope/v3.css`
5. `../202609_HumanoidSummitEurope/V3_DESIGN_BRIEF.md`
6. `../shared/trip-field/runtime.js` — HRS共通ランタイム。HRS固有の挙動は `index.html` 内のインラインスクリプトも確認
7. `index_v1.html` — 旅程内容の元データ。生成の入力なので直接編集しない
8. `index_v2.html` — 家族タブの内容・情報構造の基準
9. `build_v3.mjs`、`v3.css`、`v3.js`、`validate_v3.mjs` — 現在のv3実装

`references/rejected/transport_layout_sample.html` は無効な旧試作。参考用に隔離しているが、基準にしない。

`references/superseded/` は、現行と矛盾する旧旅程・旧スケジュールの退避先。
フォルダ直下へ戻さない。扱いは `references/superseded/README.md` に書いてある。

## 編集ルール

- 原則として `build_v3.mjs` を修正し、`node build_v3.mjs` で `index.html` を再生成する。
- `index_v1.html`、`index_v2.html`、各サンプルを直接上書きしない。
- 交通はスマートフォンでも必ず4列のままにする。
- 日付ナビ、タブ、フォントはHRS方式を維持する。
- フォントは `BIZ UDPGothic`, `Yu Gothic UI`, `Meiryo`, `system-ui`, `sans-serif`。
- 飛行機はカラー絵文字ではなく共通の `.flight-mark` を使う。
- 日単位の説明は「本日のトピックス」、動作は人物レーン、宿泊はレーン末尾に置く。
- 家族タブはv2の情報構造を維持し、会社経費や航空券見積もりを載せない。
- 不明事項は推測で断定せず、確定済みの事実と要検討事項を分ける。

## 家族タブの構成（今後の共通ルール）

家族タブは旅程タブの再掲にしない。**「どの日にどこにいて何をしているか」を
視覚的に掴めること**を最優先にする。

- 先頭付近に `どこにいるか` の一覧を置く。1日1行で、**都市名を最大文字**にする。
  日付と人物名はその手がかりに徹し、小さくする。別行動の日は人物ごとに分け、
  合流後は1つにまとめて、合流したことが見た目で分かるようにする。
- 時差は、タイムゾーン略号やUTCオフセットではなく**時差の数字を最大文字**にする
  （`−7時間` を38px、`CEST・UTC+2` は補足の12px）。家族が使うのは差の方。
- 日別の詳細は家族視点に圧縮する。載せるのは、居場所が変わる出来事
  （フライト、到着、チェックイン）、日中なにをしているか、
  **連絡が取りにくい時間帯（機内）**だけ。駅から駅への移動は載せない。
- タグにイベント種別（TechEx / EuroBLECH / 工場見学）を持ち込まない。
  家族から見れば全部「仕事」なのでまとめる。
- フライト情報ブロックは現状の作りを維持する。

2026-08-14の実測: 日別詳細は52行→31行、家族タブ全体は393px幅で
10350px→9708pxになり、`どこにいるか`（1283px）を追加した。
`validate_v3.mjs` で行数の上限と、都市名・時差の文字サイズを検査している。

## 文章の調子（今後の共通ルール）

このページは現地で見るフィールドガイドであって、読み物ではない。

- **常時表示の補足行は30字程度までの事実に留める。** 理由、判断材料、条件は
  折り畳みの中へ入れる。地の文で理屈を説明しない。
- **常時表示行の末尾に句点を付けない。** ラベルであって文ではない。
  複数文になる場合だけ文間に句点を使い、末尾には付けない。折り畳みの中は対象外。
- 2026-08-14時点の実測: 旅程タブの常時表示テキストは26行→21行、平均37字→27字、
  末尾に句点が付いた行は0件。

## 地図リンクの張り方（今後の共通ルール）

**地図リンクは場所名そのものに張る。** 「地図」「MAP」といった別リンクを横に出さない。

- 交通の出発・到着地、宿泊のホテル名を、いずれも `<a class="place">` で包む。
- `機内` や `帰宅` のように実在の場所を指さない語にはリンクを張らない。
- 見た目は交通の場所名と同じ（`#2A5FA0`・太さ600・下線なし）にそろえる。

## 乗り継ぎ・待ち時間の書き方（今後の共通ルール）

乗り継ぎと出発待ちの折り畳みは、**`やること` と `過ごし方` の2つだけ。**
特定の過ごし方を既定にしない。

- 主表示は `🕐 <地点>で乗り継ぎ（<所要>）` だけにする。理由も手順も主表示に書かない。
- 所要の書き方は交通手段の欄と同じ規則にそろえる。**推定には `約` を付け、
  時刻表どおりの区間には付けない。** セントレアの出発待ちは起点が「空港到着目安」
  なので `約3時間`、香港の乗り継ぎは前後とも定期便なので `3時間45分`。
- **`やること` は、やらないと困ることだけ。** チェックイン、保安検査、出国審査、
  搭乗口の確認、セキュリティ再検査、**コンコース間の移動**、Visit Japan Webの準備。
  館内の移動は過ごし方ではなく、目的地へ着くための手順なのでここに入れる。
- **`過ごし方` は、空き時間の使い道。** ラウンジ、無料シャワー、飲食、休憩場所を
  同じ階層に並べる。どれも並列で、ラウンジを既定にも推奨にもしない。
- **ラウンジ専用の見出しを作らない。** `ラウンジで過ごす` `ラウンジを使わずに過ごす`
  という2本立てはやめた。ラウンジは `過ごし方` の中の1行で、4系統がその下にぶら下がる
  （`.opt-sub`）。
- 4系統はこの順で必ず全部並べる。どれかを既定にも推奨にもしない。
  1. `エアライン`（搭乗クラス・航空会社ステータス・招待条件）
  2. `Priority Pass`（会員資格）
  3. `一般有料`（会員資格なしで買える）
  4. `カード付帯`（クレジットカードの特典）
- 旅程に書くのは4系統の名前と営業時間まで。**利用資格は準備タブが持つ。**
  確認できていない系統は、系統ごと消さずに「未確認」と書いて残す。
- 生成は `build_v3.mjs` の `todoFold()` / `spendFold()` / `loungeOption()` の3つだけを使う。
  同じ形のHTMLを別の場所に手書きしない。

2026-08-14時点で、この形にしてあるのは次の6箇所。
10/17（村上・セントレア約3時間、香港3時間45分）、10/18（美馬・金築・セントレア約3時間、
香港4時間25分）、10/24（全員・フランクフルト空港3時間）、10/25（全員・香港2時間15分）。
`やること` はこの6箇所に10/25の入国手続きを足した7箇所にある。

## 到着と乗り継ぎは分ける（今後の共通ルール）

**「◯◯着」と「◯◯で乗り継ぎ」を1行にまとめない。** 別の出来事なので行を分ける。

- 10/25の香港は `香港国際空港（HKG）着・乗り継ぎ（2時間15分）` という1行だったが、
  ここだけ他日と形が違っていた。`香港国際空港（HKG）着`（07:20）と
  `香港で乗り継ぎ（2時間15分）`（07:20〜09:35）の2行に分けた。
- 到着行のアイコンは、入国手続きがある日（10/18 AMS・10/19 FRA）は `🛂`、
  乗り継ぎだけの日は共通の `.flight-mark`。

## EuroBLECHの日ラベル（今後の共通ルール）

**EuroBLECHの日には番号を振らない。** 元資料にあるのは `10/20開幕` だけで、
会期が何日間かは未確認。`Day 1` も `最終` も裏が取れていないうえ、
`最終` は会期の最終日なのか自分たちの視察最終日なのかが読み手に分からない。

- 旅程の行は3日とも `🏛 EuroBLECH` ＋ `<誰>が視察。会場は<ハノーファーメッセ>` でそろえる。
- どの日に誰が行くかは日付カードのヘッダーで分かる。行に持ち込まない。
- 家族タブも `EuroBLECH 展示会視察` で統一する。
- 会期日数を一次情報で確認できたら、そのときに番号を入れるか決める。

## 用語の決定

- EuroBLECH: **展示会視察**。`工場見学`とは書かない。
- TechEx Europe: **参加**。
- Mercedes-Benz Werk Bremen: **工場見学**。見学枠 `10/22 12:45〜14:00` は予約確定。
- Bremen Hbfから工場までは**タクシー（Uber）で確定**（所要は当日確認）。復路の列車は未確定。
- Autostadt: **見学**。
- 荷物: `荷物受取`、`ホテルに荷物を預ける` に統一する。
- 宿泊欄: `機内泊`ではなく `機内`。家族タブの文章内はv2踏襲なら `機内泊`でもよい。

## 現在までに反映済み

- HRS型の5タブ、日付ナビ、BIZ UDPGothic、4列交通表示。
- 人物別レーン、日トピックス、宿泊の配置。
- 10/18村上のホテルチェックイン。
- TechEx Day 1/Day 2の時間、10/20村上の20:30頃ホテル着。
- 10/20・10/21のEuroBLECH後の戻り目安。
- 10/23のフランクフルト中央駅17:14着、17:30頃チェックイン、18:30頃夕食。
- 10/24の手続き配置、10/25香港着・セントレア到着後の入国/税関・Visit Japan Web。
- 家族タブの冗長な日時を整理し、フライト、地上移動、到着/乗り継ぎ、手続き、要検討を区別。
- 表記監査で指摘されたゼロ埋め、`Day 1`/`Day 2`、`VIP Networking Drinks`、`荷物受取`、`機内`、日本語区切りの多くを生成時に統一。

## 未完了だった6項目 — 2026-08-14に全件反映

引き継ぎ作成時の「未完了」6項目は、すべて `build_v3.mjs` 側で反映して再生成済み。
以下は反映内容と、次に触る人が前提を取り違えないための注記。

### 1. 10/17 香港ラウンジ（反映済み）

「営業時間が未確認」という記述をすべて削除し、キャセイ公式のラウンジ個別ページで
確認した営業時間に置き換えた。確認日は2026-08-14。

- The Deck: Terminal 1・Level 7・Gate 6付近・05:30〜00:30（毎日）
- The Pier, Business: Terminal 1・Level 6・Gate 65付近・05:30〜00:30（毎日）・シャワー14室
- The Bridge: Terminal 1・Level 6・Gate 35付近・05:00〜最終キャセイ便（00:30〜03:20の範囲）
- oneworld Sapphire: ビジネスクラスラウンジのみ本人＋同伴1名。ファーストクラスラウンジは対象外。
  キャセイ運航ラウンジに限られ、キャセイ／oneworld便の利用が条件。

**`The Wing` の扱いを変更した。** 現行のHKGラウンジ一覧に載っているのは `The Wing, First` だけで、
`The Wing, Business` の掲載が無い。よって村上が使えるラウンジの列挙から `The Wing` を外した。
引き継ぎ作成時にあった「The Wing, Businessは改装休業中で2027年再開予定」という記述は、
今回の確認では裏が取れていない（一覧に無いことしか分からない）ため、ページには書いていない。

空港の無料シャワーは香港国際空港の公式ページで確認した。Arrivals Level（L5）の
Gate 12付近とGate 43付近、24時間、シャンプー等は無料、タオル等は自販機で購入。

Plaza Premiumは運営会社の公式ページで確認した。Gate 60が24時間・HKD 250〜、
Gate 1が24時間・HKD 650〜、Gate 35が06:00〜01:00・HKD 650〜。
Plaza Premium Firstは East Hall（Gate 1）06:30〜01:00、West Hall（Gate 35）06:00〜01:00・HKD 980〜。

**Priority Passは依然として未確認。** Plaza Premiumの公式ページにPriority Passの記載が無く、
Priority Pass側でも確認していない。ページ上は「一般有料ラウンジ」として扱い、
「Priority Passの対象施設かは未確認」と明示してある。**Plaza PremiumをPP対象と断定しないこと。**

### 2. 10/18村上の到着（反映済み）

到着日の人物レーン先頭を `06:55` ＋ `Amsterdam Airport Schiphol（AMS）着` の主表示、
`入国審査・荷物受取` の従表示という2段にした。美馬・金築の `Frankfurt Airport（FRA）着` も
同じ形にそろえてある。日跨ぎ便の前日側4列交通はそのまま残している。

### 3. CX539のチェックイン表現（反映済み）

村上10/17と美馬・金築10/18の両方を
`国際線のため3時間前にチェックイン。自動手荷物預けの対応可否は未確認のため、有人カウンターで預ける前提で動く`
に統一した。**自動手荷物預けの対象便かどうかは確認していない。** 断定を足さないこと。
中部国際空港のカウンター位置（T1 Iカウンター等）も未確認のため書いていない。

同一文を複数箇所へ当てる必要があったので、`SOURCE_TEXT_REPLACEMENTS` の
第3要素に `'all'` を指定すると全件置換になる仕組みを足した。

### 4. 10/22の色（反映済み）

**日ヘッダーの色は変えていない。** `data-kind="visit"` のラベンダーはHRSの色契約なので、
ここを崩すと標準をHRSへ戻すときに影響が出る。代わりに凡例側を直した。

- `展示会` → `展示会視察`
- `工場・企業見学` → `工場・企業見学（予約確定）`
- オレンジの `要検討（この帯だけが未定）` を凡例に追加（`.swatch-review` を `v3.css` に追加）

未定色の手がかりが凡例に無かったことが、ラベンダーを未定色と読ませていた原因。
10/22の未定は、復路2案のオレンジの `.route-review` 2本だけが示す。
現地交通はタクシーで確定したので、時刻が目安であることを示す点線の `.route-estimate` へ移した。

### 5. EuroBLECHの文言（反映済み）

説明語を `参加` から `展示会視察`／`視察` へ統一した。アイコンも工場から展示会
（`line-icon-event`）へ変更した。工場アイコンはMercedes工場見学の3箇所だけに残っている。

`TechEx Europe: 参加` と `Mercedes-Benz Werk Bremen: 工場見学` はそのまま。
イベント固有名のラベル `EuroBLECH` も変えていない。

### 6. アイコン監査（反映済み）

生成物での実測値。`fa-*` 0件、Font Awesomeの読み込み0件、カラー飛行機絵文字0件、
フライトは共通 `.flight-mark` が14件、`line-icon-event` 8件、`line-icon-factory` 3件。

## 交通行と操作のルール（今後の共通ルール）

### 交通手段はすべてアイコン

フライトだけ絵で他が矢印、という不揃いにしない。中央列は必ずアイコンにする。

- フライトは共通の `.flight-mark`、それ以外は `.mode-icon`。寸法と色をそろえる。
- 鉄道 / 徒歩 / 車（タクシー・Uber）/ 手段未定 を `modeKind()` で振り分ける。
- **決まっていない手段を、決まっているように描かない。** 手段そのものが未定のときだけ
  `unknown`（?）にして色も要検討のオレンジにする。便だけ未定（「列車候補を確認」）は鉄道でよい。

### 帯の色と線種は確定度を表す

凡例に載せた意味のとおりに使い分ける。日ヘッダーの色は予定の**種類**であって確定度ではない。

- `route-review`（オレンジ）= 手段や便が未定
- `route-estimate`（点線）= 手段は決まっていて時刻だけが目安
- 手段が決まったらオレンジから点線へ移す。10/22のタクシー（Uber）確定がその例。

### 日跨ぎ便で着いた日は「◯◯着」を出す

到着便の4列交通は前日側にあるため、到着日の人物レーンだけを見ると着いたことが
分からなくなる。日跨ぎ到着3件（10/18 AMS、10/19 FRA、10/25 HKG）は
その日の先頭に `◯◯着` を出す。同日中の到着は直前の4列交通が示すので付けない。

### 日付カードの一括開閉

旅程タブの先頭に「すべて閉じる／すべて開く」を置く。日付カード専用の操作なので、
全タブに出るヘッダーには置かない。ラベルは現在の状態を示し、個別開閉や
日付チップの操作にも追随する。保存はしない（初期状態は全部開く）。

## ラウンジ情報の集約（2026-08-14に実施）

ラウンジは**空港で過ごす選択肢**であり、会場タブ（展示会場・工場の情報）とは
性質が違うので、**準備タブが置き場所として正しい**。この方針で全区間を集約した。

- **準備タブ**が利用資格の唯一の置き場所。「ラウンジ利用可否」カードの表を、
  区間ごとの `.lounge-seg` ブロックへ組み直した。6区間（10/17セントレア・10/17 HKG・
  10/18セントレア・10/18 HKG・10/24 FRA・10/25 HKG）が、いずれも
  `エアライン / Priority Pass / 一般有料 / カード付帯` の4系統をこの順で持つ。
  系統ごとに `利用可` か `未確認` を出す。**確認できていない系統を消さない。**
  **`未確認` を「使えない」と読み替えない。**
- **旅程タブ**は4系統の名前と営業時間だけを残し、末尾に
  `利用資格は準備タブの「ラウンジ利用可否」に集約してある` を置いた。6箇所すべて同じ文。
  「会員本人のアプリで確認する」「保有カードの特典ページで確認する」といった
  資格確認の手順は旅程から外し、準備タブへ移した。
- 内容は移しただけで、事実は足していない。Priority Passの対象施設は依然として
  セントレア・香港・フランクフルトとも未確認。**Plaza PremiumをPP対象と断定しないこと。**
- 表のセルを直接書き換えていた `SOURCE_TEXT_REPLACEMENTS` の6件は、二重管理になるため削除した。
  ブロックの定義元は `build_v3.mjs` の `LOUNGE_SEGMENTS`。
  カードか表が見つからない場合はビルドが `Lounge card is missing` / `Lounge table is missing` で止まる。

## 会場そのものへの地図リンク（2026-08-14に実施）

交通行のendpointは降車地（`Hannover Messe/Laatzen` は駅）を指す。これは正しいので変えない。
会場そのものへの導線は、EuroBLECHの3日すべてに `会場は<場所名>` の形で張った。

- 10/21（既存）、10/20、10/23。いずれも `mapLink('ハノーファーメッセ')` を使う共通形。
- 10/20は元資料に `📍 ハノーファーメッセ` という別リンクの旧形式が入っていたので、
  行ごと差し替えて共通形にそろえた。
- TechExは駅名＝会場名なので現状で足りている。

## 準備タブの表が切れる件（2026-08-14に修正）

`.legacy-tab table` を `div.table-scroll`（`overflow-x:auto`）で包み、
表だけを横スクロールできるようにした。印刷時は `overflow:visible` に戻す。

引き継ぎ時の記述を2点訂正する。

- 切れていたのは「ラウンジ利用可否」ではなく**「航空券状況」の5列表**。
- 原因は `div.bg-white.rounded-xl` のTailwind由来 `overflow-x: hidden` ではない。
  Tailwindは読み込んでいないのでそれらのクラス名は効いていない。実際は
  `v3.css` の `.legacy-stack>.bg-white{overflow:hidden}`（角丸を保つための指定）。
- 「412px以上では発生しない」も誤り。表の最小幅は389pxで、412px幅でも
  内側の実効幅は360pxしかないため切れていた。

## 次にやること — HRSからEBへ戻す（2026-08-15に棚卸し）

2026-08-15までHRS側を作り込んだ。ここからEBへ戻す段階に入る。
下の数値は**同日に `index.html` と `v3.css` を実際に数えた実測値**である。
着手前に数え直すこと。数えずに「完了」と書かない。

**この作業はEBだけのものではない。** 2026年12月のSIGGRAPH Asia、2027年3月のLOGIMATを
同じ基盤で作る。**EBへ当てるものは、まず `shared/trip-field` に置けないかを考える。**
EB側のファイルに書いた分は、次の2イベントで同じ作業をもう一度やることになる。
実例がある。アイコン41種・`.plan-state`・`.line-icon` の `em` 化は、いずれも
一度どちらかのイベント側へ書いてしまい、共通へ引き出す二度手間になった。

判断の目安は `../shared/trip-field/README.md` の Shared versus event-specific。
配色・旅程の中身・人のレーンはイベント固有。アイコンの形と寸法、状態の語彙、
句読点、折り畳みの挙動、記録と書き出しの契約は共通。

### EBとHRSの構造の違い（作業量に効く）

| | HRS | EB |
|---|---|---|
| 生成物 | 4件（`index.html` / 机上用印刷版 / 家族向け印刷版 / `v3.css`） | **1件（`index.html` のみ）** |
| CSS | 自前の `v3.css`（`core.css` を連結） | HRSの `v3.css` を読み込み ＋ 自前の `v3.css` |
| 補足の折り畳み | `.note` → `details.fold` 20件 | `details.fold` 21件（やること7・過ごし方6・食事メモ8）。**EBが元祖なので移植不要** |
| 補足のマークアップ | `.note` | Tailwind風（`text-slate-500 text-xs` など）。`.note` は0件 |
| 予定の状態 | `.plan-state` 9件 | **札が無い。** 語が地の文に散る（候補12・確定11・未定5・要検討2・要確認5） |

**EBはHRSの `v3.css` を読むので、`.plan-state`・`.line-icon`（`1.15em`）・アイコン41種は
すでに届いている。** 必要なのはマークアップの変更と、EB側の上書き削除だけ。

### 着手前に決める項目

1. **机上用印刷版と家族向け印刷版をEBにも作るか。** HRSは4生成物、EBは1件。
   EBの家族向けは `index.html` 内のタブのまま
2. **家族タブの構成。** EBは `family-where`（`🗺 どこにいるか — 9日間の全体像`）/
   `family-flights` / `family-schedule` の3構成。HRSで確定した5構成は
   出張サマリー / 時差・気候 / 日程詳細 / 宿泊先情報 / 緊急連絡先。
   **HRSでは `family-where` に当たるブロックを「日程詳細と重複」として削除した。**
   EBでも同じ判断にするか
3. **Tailwind風マークアップを共通CSSへ寄せる範囲。** `border-l-4` 28件、
   `text-slate-*` `bg-slate-*` 多数。緊急連絡先だけ直すのか、全体を寄せるのか
4. **5状態をどの予定に付けるか。** EBには札が無く、30語が地の文にある。
   どれを `未検討 / 候補あり / 仮決め / 確定 / 当日判断` のどれにするかは中身の判断。
   HRSでの対応付けの考え方は `../202609_HumanoidSummitEurope/CLAUDE_HANDOFF.md` の
   「予定の状態は5つ」にある（進み具合の軸と、未確定の理由の軸を混ぜない）
5. **`📍` 22件の扱い。** 下の「既存の未決」を参照。10/22だけか、全部か
6. **緊急時番号 `+49 30 21094-222`（2件）。** 外務省の在外公館リストに無い。
   代表番号 `210940` は一致する。裏が取れなければ落とす

### 判断済みの作業項目

1. **絵文字の全廃** — 29種123箇所 ＋ 国旗17組（34文字）。生成物に1文字も残さない
   （規約は `../CLAUDE.md`）。国旗は描かずに落とす
   - **12種81箇所はHRSのアイコンでそのまま賄える**（📍22 🎯13 ✅12 👥9 ⚠8 ⭐5 📅4 👤4 📝 🏠 📱 🆘）
   - 残り17種42箇所のうち、🗺2→既存 `map`、🚆1→既存 `train`、🕗1→既存 `clock` を流用。
     **実際に描くのは14種**（🛒14 ✍6 🎫3 🌐3 🏢2 📞2 💰 💻 🔍 💳 🌍 🚑 📁 📄）
   - 仕組みはHRSの `build_v3.mjs` にある `EMOJI_ICON_MAP` / `dropFlags` /
     `stripScriptEmoji` / `applyEmojiIcons` をそのまま持ってこられる。
     対応表に無い絵文字が現れたらビルドが止まる作りにしてある
2. **`v3.css` の `.line-icon` 固定19pxの複製を削除** — 共通定義（`core.css`、`1.15em`）に
   任せる。EBの `.line-icon` は56件あり、そのうち44件が13〜14pxの文字の中で
   比1.36〜1.46になっている。色の上書き（`[class*="bg-gray-700"]`）だけ残す
3. **モバイルの日付列幅 58px → 64px**（`v3.css` に1件）。HRSでは2桁日付が折り返した
4. **家族向けフライトの `予約クラス` 8件を削除** — 家族には不要
5. **緊急連絡先を `.family-section` 様式へ** — いまはTailwind風の旧マークアップ

### 既存の未決

**10/22 復路の二択 — ユーザーの判断待ち。**
早帰り案（16:00頃発）と市内滞在案（18:00頃発）を交通行2本で並べてある。
**どちらか選んだら、もう片方の行を消す。** 便が決まるまでは `route-review`（オレンジ）のまま。

**`📍` 付きリンクが規約と不一致 — 未着手。**
10/22の旅程に `📍 Mercedes-Benz Werk Bremen`、`📍 マルクト広場・市庁舎（世界遺産）`、
`📍 ブレーメンの音楽隊像`、`📍 シュノーア地区（路地）`、`📍 ベットヒャー通り` がある。
「地図リンクは場所名そのものに張る」という共通ルールに対して、`📍` 付きのまま。
**ページ全体では22件**（会場タブ・家族タブを含む。2026-08-15の実測）。
`>地図</a>` `>（地図）</a>` の別リンク形式は0件なので、直す対象は
「リンク文の先頭に付いた `📍`」であって別リンクではない。
どこまでそろえるかは未決だが、絵文字の全廃（作業項目1）と対象が重なる。

## 検証

変更後は次を実行する。

```powershell
node .\202610_Europe_TechEx_EuroBLECH\build_v3.mjs
node .\202610_Europe_TechEx_EuroBLECH\validate_v3.mjs
git diff --check
```

`scripts/validate_event_page.mjs` はこのチェックアウトには存在しない。追加されている環境では併用する。

`build_v3.mjs` の置換文字列はLFで書いてある。リポジトリは `.gitattributes` の `* text=auto` と
`core.autocrlf=true` により作業ツリーがCRLFになるため、`index_v1.html` と `index_v2.html` は
読み込み時にLFへ正規化してから照合する。この正規化を外すと `Missing source text replacement` で
ビルドが止まる。生成物はLFで書き出されるので、直後の `git status` に `index.html` が
修正扱いで出ることがあるが、`git diff --numstat` が0行なら内容の差分はない。

その後、localhostで `index.html` を開き、390pxと約1200pxで次を実機確認する。

- 横はみ出しが0件
- 4列交通が崩れない
- 長い駅名/ホテル名/時刻が欠けない
- フライトアイコンが黒いカラー絵文字にならない
- 5タブと日付チップが動く
- 10/22の確定/未定の色が誤解を生まない
- 家族タブの時刻とタグが読みやすい

最終的に、再生成した `index.html` とブラウザ確認したファイルが同一であることを確認する。

## Gitの注意

- 直近の作業ブランチは `claude/euroblech-folder-handover-check-666081`（worktree）。
- HRSのビルドを走らせると `202609_HumanoidSummitEurope/` の生成物4件が
  改行コードだけで modified 扱いになる。`git diff --numstat` が0行なら
  `git checkout --` で戻してよい。EUROBLECHのコミットに巻き込まない。
- `references/rejected/transport_layout_sample.html` は無効な旧試作として隔離。対象フォルダ直下へ戻さない。
- `index_v1.html` は変更しない。
- ワークツリーに別作業の変更があれば巻き込まない。
