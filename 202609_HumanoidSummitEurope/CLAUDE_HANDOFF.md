# Claude 引き継ぎ — HRS Europe 2026 field guide v3

更新日: 2026-08-15

## 進め方

**EUROBLECH（`../202610_Europe_TechEx_EuroBLECH`）を参考にしてHRSを作り込み、
必要なものをEUROBLECHへ戻す。** 2026-08-14まではEUROBLECHで仕様を確定して
HRSへ戻していた。作り込む場所が入れ替わっただけで、目的は変わっていない。

**EUROBLECHへ何を戻すかは、HRSが固まってから決まる。**
HRSが未完のうちにEUROBLECH側の文書やコードを組み替えない。作り直しになる。

### 着手前に必ずやること

**EUROBLECHの実マークアップを読んでから設計する。** 要約や記憶で組み立てない。
2026-08-15の作業で、読まずに設計して作り直した箇所が4件ある。

- `route-four`（4列交通）— 時刻列をグリッド外に出し、ラベルに時刻を混ぜていた
- `family-where`（どこにいるか）— カード書式で組み、クラス名も独自にしていた
- `timezone-cards`（時差）— 1枚に詰め込み、ゾーン別カードにしていなかった
- `family-schedule`（日程詳細）— 俯瞰ブロックを日程詳細と取り違えていた

いずれも「EUROBLECHの数値だけ合わせて構造が違う」という同じ誤りである。
`202610_Europe_TechEx_EuroBLECH/index.html` の該当セクションと `v3.css` の
定義を実際に開き、要素構成・クラス名・寸法を写すこと。

## ファイル構成

| ファイル | 役割 |
| --- | --- |
| `index.html` | **v3の生成物。フォルダの公開入口。** 直接編集しない |
| `index_v3_offline.html` | 机上用印刷版の生成物。スクリプト0件を保つ |
| `family_print.html` | 家族向け印刷版の生成物 |
| `v3.css` | 生成物。`shared/trip-field/core.css` を連結して作る |
| `index_v1.html` | 旧版の退避 |
| `index_v2.html` | **生成の入力元。** 旅程・家族データ（`FAM`）の出どころ |
| `index_v3.html` | 以前の共有URLを `./` へ転送する互換ページ。生成物ではない |
| `build_v3.mjs` | 生成コード。**編集するのはここ** |
| `validate_v3.mjs` | 受入検査 |
| `V3_DESIGN_BRIEF.md` | 設計文書（英語） |
| `references/design-samples/` | v3を決めるまでの比較サンプル。基準にしない |

## 編集ルール

- **生成物を直接編集しない。** `build_v3.mjs` を直して `node build_v3.mjs` で再生成する。
- `index_v1.html`、`index_v2.html` を上書きしない。
- 置換のアンカーは、当てる前に出現回数が1件であることを確認する。`mustReplace` を使い、
  見つからなければ例外で止める。件数が変わったら気づける形にする。
- 共通機能は `../shared/trip-field` に置く。HRS固有の外観はHRS側に残す。

## HRS固有の判断と、その理由

理由まで残す。理由が無いと、次の担当者が「揃っていない」と見て戻してしまう。

### 主要タブは3つ（旅程 / 会場 / 記録）

家族と準備はタブにしない。家族は `family_print.html` が持つ。
EUROBLECHは5タブだが、HRSは3タブを維持する。

### 旅程は表ではなく divスタック

`<table class="tl">` をやめ、`.tl-stack` の中に `.action`（2列）と
`.route-four`（4列）を並べる。**両者の第1列を同じ幅にすることで、
2列の行と4列の行で時刻が縦にそろう。** `<table>` の `colspan` ではこの整列が作れない。

時刻列は112px。表のときは列が内容に合わせて自動で広がったが、gridは広がらない。
`13:00〜14:00` が112px要るので、そこに合わせたうえで折り返しも許している。

### 4列化はフライト4区間のみ

フライトは `X発 → Y（便名・機材・所要）` という一定の形なので分解できた。
鉄道37件と徒歩の行は出発地・到着地・所要が1つの文に入っており、
分解は内容の書き換えになる。**やるかどうか自体が未決。**

到着行（`HEL 着` など）は乗継や入国手続きを持つ別の出来事なのでまとめない。

### 家族印刷版は5セクション

`出張サマリー / 時差・気候 / 日程詳細（家族向け） / 宿泊先情報 / 緊急連絡先`。

家族が最初に知りたいのは「どの日にどこにいて、いつ連絡が取れないか」。
以前は時差の説明と8行の表が同じ重さで並び、それが読み取れなかった。

### 時刻は現地時刻が既定、日本時間は明記して併記する

EUROBLECHは「時刻はすべてその場所の現地時刻」と宣言して換算しない。
**HRSは日本時間への換算も残す。** 家族が自分の時計で読めるようにするため。
`.timezone-lead` で読み方を宣言する。機内（連絡が取れない時間帯）は日本時間で出す。

### 時差カードのゾーン名に国旗絵文字を使わない

EUROBLECHは `🇯🇵 日本` としているが、Windowsでは国旗に合成されず `JP` `DE` と
文字で表示される。実機で確認済み。`日本` `ドイツ` の文字だけにする。

### 既定表示が「文字」の絵文字を使わない

`🗓` U+1F5D3、`🖨` U+1F5A8、`🗺` U+1F5FA は既定が文字表示なので、
異体字セレクタ無しだと黒い輪郭グリフになる。EUROBLECHが持つ
`line-icon` のモノクロSVGへ置き換える。交通アイコンをSVGに統一したのと同じ理屈。

### 「＋詳細」トグルは廃止

補足を既定で隠すのは現地で見落としの元になる。EUROBLECHにも無い。
ボタンだけ消すと `.note` と `.dt` が永久に隠れるので、CSSで常時表示にした。

保存値は読み込み時に `store.del('detail')` で落とす。書き出しが除くのは
`ui:` 付きのキーだけなので、残すとバックアップJSONに毎回入り、
読み込んだ端末へも複製される。古いJSONから復活しうるので、この処理は常設。

### 長い補足は折り畳みへ

常時表示は30字程度の事実まで。理由・判断材料・条件は `details.fold` の中へ。
`summary` は中身を示す語にする（`やること` `過ごし方` `ラウンジの利用資格`）。
`続きを読む` は使わない。

**字数は目安であって基準ではない。** 30字を超えても単一の事実なら常時表示に残し、
短くても理由の説明なら畳む。分けるのは長さではなく、事実か理屈か。

規則の本体は `../CLAUDE.md`、`../OVERSEAS_TRIP_LAYOUT_SUMMARY.md` の4.5節、
`../shared/trip-field/README.md` にある。

## 検証

```powershell
node .\202609_HumanoidSummitEurope\build_v3.mjs
node .\202609_HumanoidSummitEurope\validate_v3.mjs
node .\shared\trip-field\validate-template.mjs
node .\cloudflare\trip-notes-worker\validate-worker.mjs
git diff --check
```

そのうえでローカルサーバーで **393px / 412px / 1280px** を確認する。

- 横あふれ0件、セル内の文字切れ0件
- コンソールエラー0件
- 机上用印刷版（`index_v3_offline.html`）のスクリプト0件、外部依存0件
- 家族印刷版のスクリプト0件
- 日付カードの一括開閉が動き、ラベルが個別開閉に追随する

**スクリーンショットは異常が疑われるときだけ。** 通常は数値と真偽値のテキスト出力で足りる。

`.gitignore` に `**/*.md` があるため、**新規のMarkdownは追跡対象外になる。**
この文書のように残したい文書は `git add -f` でステージする。

## 確認できていない事実

断定を避けて書いていない。埋めるときは一次情報を読んでから。

- **在外公館の管轄区域** — 外務省の在外公館リストに記載が無い。
  シュトゥットガルトとフランクフルトをどの公館が管轄するかは未確認。
  ページには大使館と在フランクフルト総領事館を併記し、管轄は書いていない。

気候の値（名古屋・京都・シュトゥットガルト・フランクフルト）はいずれも
出典を開いて確認済み。統計期間は都市ごとに違うが、これは未確認事項ではない。
ページには出典名と期間を書き、それ以上の注記はしない。

**外務省と大使館の公式サイトはWebFetchが403を返す。** 実ブラウザーでは読めた。
政府サイトを当たるときは最初からブラウザーを使う。

## 表示不具合2件（2026-08-15に実機で発見、同日修正）

コミット `22451d8` の時点で入っていた。**数値検査は通るのに、画面が正常ではなかった。**
横あふれ0件・文字切れ0件だけを見ていたため取り逃がした。
**幅と高さの比、文字サイズとの相対関係も測ること。**

### 1. 気候の表が393pxで潰れる（家族印刷版）→ 表をやめた

`時差・気候` セクションの気候表。393px幅での実測は次のとおりだった。

| 列 | 幅 | 行高 |
|---|---|---|
| 地点・月 | 43px | 74px |
| 平均最高 | 66px | 74px |
| 平均最低 | 64px | 74px |
| 出典・期間 | 168px | 74px |

見出しが1文字ずつ縦に折り返していた。**出典列が168pxを占め、家族が最も見たい
気温を押し出していた。** 原因は出典と統計期間を表の列として持たせたこと。

直した形: 表をやめて都市ごとの行（`.climate-list` / `.climate-row`）にした。
都市名15px・気温22px・出典11px。出典は消さず、重みだけ下げた。
`.timezone-card` と同じ「知りたい数字を大きく」の作り。
393px・412pxの実測で、都市名1行・気温1行（2つの気温が横並び）・出典2行、
行高112px、横あふれ0件。

### 2. `.line-icon` が文字より大きい（オンライン版）→ em基準にした

`.line-icon` は幅・高さとも**固定19px**で、置かれている場所の文字は13〜16pxだった。

`.flight-mark` と `.mode-icon`（20px）は専用の列に入るので固定値でよいが、
`.line-icon` は見出しやボタンの文中に置くので文字サイズに追従させる。
`width/height:1.15em`、`margin-right:.3em`、`vertical-align:-.2em` へ変更。
13pxの `.btn` では14.9pxになる（実測、比1.15）。

EUROBLECHは `family-section-head`（16px）の大きい文字の中で使っているため
19pxでも成立していた。**HRSがEUROBLECHと意図的に違う点。**
EUROBLECHへ戻すときは、EUROBLECH側の置き場所の文字サイズを見てから決める。

検証には比率の検査を足した（`validate_v3.mjs`）。
`.line-icon` が `em` で寸法を持つこと、気候が `<table>` でないことを見る。

## 進行中・次にやること

`../WORK_STATUS.md` に最新の状態がある。2026-08-15時点で動いていたのは次の2つ。

1. 家族印刷版から俯瞰ブロックと時差の対応表を削り、気候・緊急連絡先を入れる
2. 旅程の長い `.note` を `details.fold` へ移す

保留: 鉄道37件・徒歩の4列化（やるかどうかが未決）。

HRSが固まったら、EUROBLECHへ戻す作業に入る。戻す項目は
`../WORK_STATUS.md` の「HRSからEUROBLECHへ戻す」節にある。
