# 海外出張ページ共通レイアウト — ここまでのまとめ

更新日: 2026-08-13

## 1. 目的

このページ群は、確定済みの旅程を清書するだけのものではない。
未確定の段階から情報と候補を積み上げ、当日までに行動イメージを
具体化し、現地で参照・記録し、帰国後にPCへ保存するための道具とする。

基本の流れは次のとおり。

1. 情報を集める
2. 候補を比較する
3. 現時点の第一候補を仮決めする
4. 予約・発券・確認を経て確定する
5. 当日はスマートフォンで予定を確認し、講演中のメモを残す
6. 帰国後または出張終了時にPCへ読み込み、MarkdownとJSONを保存する

## 2. 共通レイアウト

現地で使うオンライン版の主要セクションは、イベントが変わっても
次の3つに統一する。

- `旅程`: 日ごとの連続した行動予定。日付から直接移動できる。
- `会場`: 講演、出展社、工場見学など、そのイベント固有の対象。
- `記録`: メモの確認、クラウド同期、Markdown・JSONの保存。

準備、書類、家族向け情報は現地の主要タブに増やさず、旅程内の
補助セクションまたは別ページに置く。

配色、カード形状、会場情報の構造はイベントごとに変えてよい。
共通化するのは見た目そのものではなく、操作、保存形式、スマートフォン対応、
印刷時の役割分担である。

この形式はHRSやEUROBLECHに限らず、今後作る展示会、講演会、カンファレンス、
工場見学、企業訪問の標準形式とする。記録単位はイベントに合わせて講演、
出展社・ブース、訪問先、面談などへ変えてよいが、事前準備、当日記録、
クラウド同期、PCでの最終保存という流れは共通にする。

## 3. 端末と媒体の役割

### オンライン版

- iPhone 16とPixel 7a/8aを主要対象とする。
- 現地ではスマートフォンで閲覧する。
- 44px以上の操作領域と、入力時16px以上の文字を基本とする。
- 日程、会場、記録を同じURLから扱う。
- 入力はまずブラウザ内へ即時保存する。

### 机上用印刷版

- PCやスマートフォン用のオフラインアプリではなく、机に置く紙の控え。
- 旅程、準備、会場の参照情報だけを載せる。
- メモ欄、クラウド同期、実行スクリプトは含めない。
- 単一HTMLとして印刷できるよう、必要なCSSを埋め込む。

### 家族向け印刷版

- 詳細な業務メモは載せず、日ごとの居場所と大きな予定を示す。
- スマートフォンではカード、印刷時は表として読める。

## 4. CSSとファイル構成

オンライン版では、現在の一般的な構成どおりHTML、共通CSS、テーマCSS、
JavaScriptを分離する。イベント固有の配色はテーマ側へ置く。

印刷版だけは、単一ファイルで机上に残せることを優先し、生成時にCSSを
埋め込む。昔のフレーム構成は使わず、スマートフォンでは縦スクロールと
固定ナビゲーションを使う。

共通基盤は `shared/trip-field` に置く。

- `template.html`: 新しい出張ページの開始点
- `core.css`: スマートフォン対応を含む共通部品
- `theme-template.css`: イベント固有テーマの雛形
- `runtime.js`: ローカル保存、タブ、JSON、Markdown、クラウド同期
- `build-desk-print.mjs`: 静的な机上用印刷版の生成
- `validate-template.mjs`: 共通仕様の検証

飛行機は端末ごとに色や形が変わる絵文字を避け、モノクロ文字の `✈︎` を使う。
方向はアイコンではなく `発` と `着` で示す。

## 5. 予定を育てるための状態

予定は単純な「未定／確定」だけではなく、次の段階で扱う方針とする。

- `未検討`: まだ案を作っていない
- `候補あり`: 比較する案がある
- `仮決め`: 現時点の第一候補
- `確定`: 予約、発券、公式確認などが終わった
- `当日判断`: 天候、運休、体調などで現地判断する

候補を選んでも、代替案は削除せず折りたたんで残す。確定に必要な条件、
確認期限、予約状態を合わせて表示し、当日は採用中の予定を前面に出す。

HRSでは候補選択と確定を分けるところまで実装済み。上記5段階の共通部品化は
今後の作業である。

## 6. メモと保存

講演や訪問先には担当者ボタンを置かず、次の2欄を持たせる。

- `事前の狙い・質問`
- `当日メモ`

`当日メモ` には「＋ 時刻付きで追記」を置き、端末のローカル時刻とUTCオフセットを
付けて既存内容の末尾へ追加する。以前の記録は消さず、通常の直接編集も残す。

入力はローカルに自動保存する。オンライン保存を有効にした場合も、通信失敗で
入力を失わないようローカル保存を先に行う。

PCでの最終保存手順は次のとおり。

1. PCでオンライン版を開く
2. Cloudflareから最新記録を読み込む
3. `メモをMarkdownでDL` で人が読める記録を保存する
4. `バックアップJSONをDL` で復元可能な完全データも保存する

HRSのMarkdownには、出張全体、日別記録、講演別の事前質問・当日メモ、
完了タスクが入る。

## 7. Cloudflare同期

参考にしたLSP管理ページと同様に、静的ページからCloudflare Workerへ
GET/PUTし、通信できないときもローカルデータを残す方式を採用する。

安全面では、同期キーを公開HTMLやJavaScriptへ埋め込まない。画面上で入力し、
利用者が選択した場合だけその端末内へ記憶する。Worker側では秘密情報として
管理する。

実装済み:

- 共通ランタイムの読込、保存、編集後の遅延自動保存
- 手動の `クラウドから読込` と `今すぐ保存`
- 同期キーを出張データとJSONへ混入させない処理
- Cloudflare WorkerとWorkers KVの雛形
- 許可オリジン、認証、データ形式、容量の検証

2026-08-13に実配備済み:

- Cloudflareアカウント上に専用Workers KVを作成
- Worker Secret `SYNC_TOKEN` をユーザー本人が直接登録
- `trip-field-sync` Workerをデプロイ
- Worker URL `https://trip-field-sync.mrkn55.workers.dev` をHRSへ設定
- 未認証アクセスが `401 Unauthorized` になることを確認
- GitHub Pages公開版を393pxとPC幅で確認
- ユーザーが保持する同期キーで認証付きPUT/GETを行い、保存内容の一致を確認

これによりHRS v3のクラウド同期を含む技術受入は完了した。
日別・講演別の当日メモは、時刻付きで履歴を残しながら追記できる。

Workers KVは同じ出張スナップショットを最後の保存で更新する方式である。
現段階では、村上のスマートフォンとPC間の同期を想定する。複数人が同時編集する
共同ノート用途にはしない。

## 8. HRS Europe 2026の現在地

実装済みの主なファイル:

- `202609_HumanoidSummitEurope/index.html`: 正式なスマートフォン用オンライン版
- `202609_HumanoidSummitEurope/index_v3.html`: 旧v3 URLから正式入口への転送
- `202609_HumanoidSummitEurope/index_v1.html`: v3以前の旧オンライン版
- `202609_HumanoidSummitEurope/index_v3_offline.html`: 机上用印刷版
- `202609_HumanoidSummitEurope/family_print.html`: 家族向け予定表
- `202609_HumanoidSummitEurope/v3.css`: オンライン版と家族版のCSS
- `202609_HumanoidSummitEurope/build_v3.mjs`: 生成処理
- `202609_HumanoidSummitEurope/validate_v3.mjs`: 受入検証

HRSは休日である9/12・9/13の過ごし方など、まだ決まっていない部分を候補として
残し、当日までに第一候補と確定条件を詰めていく。便とホテルが確定していても、
休日案は未確定というように、同じページ内で確度の違いを見せる。

## 9. EUROBLECHへ引き継ぐ考え方

EUROBLECHでは村上と美馬・金築の行動が数日間分かれるため、HRSとは異なり
人別の表示切替が必要になる。

想定する表示:

- `全員`
- `村上`
- `美馬・金築`

これは主要タブや担当割当ではなく、旅程の表示フィルターとする。選択した人の
行動を大きく表示し、別行動は折りたたむ。合流後の共通予定は常に表示する。
端末ごとに最後の選択を記憶する。

机上用印刷版はフィルターせず、全員の予定を並べる。家族向けは村上の居場所を
中心に簡略化する。

この人別フォーカス機能とEUROBLECHの共通基盤への移行は未実装であり、次の
主要作業となる。

## 10. 検証済み事項

- HRSオンライン版はiPhone 16相当の393px幅で横あふれなし
- Pixel 7a/8a相当の412px幅で横あふれなし
- 主要タブは `旅程／会場／記録` の3つ
- 全8日の日カードを連続して参照可能
- ブラウザ再読込後もローカル記録を保持
- JSONの書出し・読込み
- PCでのMarkdownダウンロード完了表示
- 机上用印刷版はスクリプト0件
- 共通テンプレート、HRS生成物、Workerの自動検証に合格
- ブラウザコンソールエラーなし

## 11. 次の作業

1. 予定の5段階状態と「次に決めること」を共通部品化する
2. 人別フォーカスを共通ランタイムへ追加する
3. EUROBLECHを同じ操作基盤へ移行する
4. 配色と内容構造はEUROBLECH固有のものを維持する

## 12. ここまでの主要コミット

- `49e8677` Add reusable overseas trip field layout
- `8672d69` Add optional Cloudflare trip note sync
- `d386361` Add PC download for final trip notes
